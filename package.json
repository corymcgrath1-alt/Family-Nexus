{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@11.5.3",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.5.3"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "db:test:up": "node scripts/db-test.mjs up",
    "db:test:migrate": "node scripts/db-test.mjs migrate",
    "db:test:reset": "node scripts/db-test.mjs reset",
    "db:test:down": "node scripts/db-test.mjs down",
    "test:integration": "node scripts/run-integration-tests.mjs",
    "test:e2e": "node scripts/run-e2e-tests.mjs",
    "test:connector-catalog": "pnpm --filter @workspace/api-server exec tsx --test src/lib/connector-catalog.test.ts",
    "test:library-policy": "pnpm --filter @workspace/api-server exec tsc src/lib/library-policy.ts src/lib/library-policy.test.ts --module commonjs --target es2022 --moduleResolution node --outDir ../../tmp/library-policy-test --skipLibCheck --strict --esModuleInterop --types node && node tmp/library-policy-test/library-policy.test.js",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  }
}
